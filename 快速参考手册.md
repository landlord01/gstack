# Gstack 快速参考手册

## 1. 安装与配置（3 分钟）

### 1.1 安装
```bash
git clone https://github.com/garrytan/gstack.git ~/.claude/skills/gstack
cd ~/.claude/skills/gstack && ./setup
```

### 1.2 配置 CLAUDE.md
```markdown
## gstack

使用 /browse skill 处理所有网页浏览。禁止使用 mcp__claude-in-chrome__* 工具。

可用 skills：
/plan-ceo-review, /plan-eng-review, /plan-design-review, /design-consultation,
/review, /ship, /browse, /qa, /qa-only, /setup-browser-cookies, /retro, /document-release
```

### 1.3 验证
```bash
cd ~/.claude/skills/gstack && bun run skill:check
```

---

## 2. 常见工作流程

### 流程 1：新功能开发
```
/plan-ceo-review      # 重新思考问题
    ↓
/plan-design-review   # 检查设计
    ↓
/plan-eng-review      # 锁定架构
    ↓
[Claude 编码 ~30 分钟]
    ↓
/review               # 代码审查
    ↓
/qa <staging-url>     # QA 测试
    ↓
/ship                 # 发布 PR
```

### 流程 2：Bug 修复
```
/qa <url>             # 查找 bug
    ↓
[Claude 编码修复]
    ↓
/review               # 审查
    ↓
/qa <staging-url>     # 验证
    ↓
/ship                 # 发布
```

### 流程 3：代码审查
```
/review               # 分析 diff
    ↓ 自动修复明显问题
    ↓
[手动检查需要确认的问题]
```

---

## 3. Browse 命令速查表

### 导航
```bash
$B goto <url>              # 跳转到 URL
$B back                    # 返回
$B forward                 # 前进
$B reload                  # 重新加载
$B url                     # 显示当前 URL
```

### 交互
```bash
$B snapshot                # 显示可交互元素（@refs）
$B click @e1               # 点击元素
$B fill @e2 "text"        # 填充输入框
$B select @e3 "option"    # 选择下拉
$B type "text"            # 逐字符键入
$B press Enter            # 按键
$B upload @e1 /path/file  # 上传文件
$B scroll @e1             # 滚动到元素
$B hover @e1              # 鼠标悬停
$B wait 1000              # 等待 1 秒
```

### 读取
```bash
$B text @e1                # 获取元素文本
$B html @e1               # 获取 HTML
$B links                   # 所有链接
$B forms                   # 所有表单字段
$B attrs @e1 class id    # 获取属性
$B is @e1 visible        # 检查状态
$B css @e1               # 计算样式
$B console               # JavaScript 错误
$B network               # 网络请求
$B cookies               # 所有 cookies
$B storage sessionId     # localStorage/sessionStorage
```

### 可视化
```bash
$B snapshot -i             # 仅交互元素
$B snapshot -D             # 对比快照
$B snapshot -a -o out.png # 标注 refs
$B snapshot -C             # 光标可交互元素
$B screenshot              # 整页截图
$B screenshot --clip x,y,w,h out.png  # 裁剪
$B screenshot @e1 out.png # 特定元素
$B pdf output.pdf         # PDF 导出
$B responsive             # 响应式测试
```

### 高级
```bash
$B diff <url1> <url2>     # 对比两个 URL
$B js "document.title"    # 执行 JavaScript
$B eval "1 + 1"          # 评估表达式
$B tabs                   # 列出标签页
$B tab 2                  # 切换到标签页 2
$B newtab <url>          # 打开新标签页
$B closetab 2            # 关闭标签页
$B dialog-accept         # 接受 alert
$B dialog-dismiss        # 拒绝 alert
$B chain < commands.json # 批量命令
$B help                  # 所有命令
```

### 调试技巧
```bash
# 看不到某个元素？
$B snapshot -i                    # 查看交互元素
$B snapshot -C                    # 包含光标可交互

# 验证操作成功？
$B snapshot -D                    # 建立基线
$B click @e3                      # 执行操作
$B snapshot -D                    # 对比变化

# 哪个元素是哪个？
$B snapshot -a -o debug.png      # 屏幕上标注
open debug.png                   # 查看标注

# 页面有错误？
$B console                        # 查看错误
$B network                        # 查看请求
```

---

## 4. Skill 选择决策树

```
我想...

├─ 重新思考一个功能
│  └─ /plan-ceo-review
│
├─ 检查设计是否"AI Slop"
│  └─ /plan-design-review (仅报告)
│     或 /design-review (修复)
│
├─ 设计新的设计系统
│  └─ /design-consultation
│
├─ 锁定架构和数据流
│  └─ /plan-eng-review
│
├─ 审查代码找隐藏的 bug
│  └─ /review
│
├─ 在浏览器中测试并修复 bug
│  └─ /qa <staging-url>
│
├─ 仅测试不修复
│  └─ /qa-only <staging-url>
│
├─ 发布到 main
│  └─ /ship
│
├─ 在网页上执行操作
│  └─ /browse (使用 $B 命令)
│
├─ 为 QA 导入已登录状态
│  └─ /setup-browser-cookies
│
├─ 查看每周统计
│  └─ /retro
│
└─ 更新过期的文档
   └─ /document-release
```

---

## 5. 关键术语解释

| 术语 | 含义 |
|------|------|
| **@e1, @e2** | ARIA 树中的元素引用（无需 CSS 选择器） |
| **@c1, @c2** | 光标可交互元素引用（ARIA 树中缺失但可点击） |
| **AI Slop** | AI 生成的设计陈词滥调（渐变英雄、默认图标、统一圆角） |
| **Boil the Lake** | 完整性原则 - AI 使边际成本接近零，所以总是做完整实现 |
| **Ref 过期** | 元素不再存在时，需要运行新的 snapshot |
| **SPA 导航** | 路由变化但无页面刷新（React Router 等） |
| **Bearer Token** | 服务器认证，存储在 .gstack/browse.json 中 |
| **Conductor** | 运行 10 个并行 Claude Code 会话的工具 |

---

## 6. 性能基准

| 操作 | 时间 |
|------|------|
| 首次 Chromium 启动 | ~3 秒 |
| 后续浏览器命令 | 100-200ms |
| snapshot | 50-100ms |
| screenshot | 100-200ms |
| 导航到新 URL | 1-2 秒（取决于页面） |
| 点击 + 等待导航 | 1-2 秒 |
| 5 个命令总计 | ~1-2 秒（vs 2-3 秒/命令如果每次启动） |

---

## 7. 常见错误排查

| 错误 | 原因 | 修复 |
|------|------|------|
| "Skills 不显示" | 未运行 setup | `cd ~/.claude/skills/gstack && ./setup` |
| "Ref @e3 已过期" | SPA 更新 | `$B snapshot` 获取新 refs |
| "浏览器超时" | Chromium 卡顿 | `rm .gstack/browse.json` 然后重试 |
| "Cookie 导入失败" | 浏览器未找到 | 手动导出 JSON 用 `$B cookie-import` |
| "命令返回空" | 页面还在加载 | `$B wait 2000` 然后重试 |
| "点击未起作用" | 使用了 CSS 选择器 | 用 `@e1` ref 代替 |

---

## 8. 快速命令模板

### 完整 QA 流程
```bash
$B goto https://staging.myapp.com
$B snapshot -i
$B click @e1                    # 登录按钮
$B fill @e2 "user@example.com"
$B fill @e3 "password"
$B click @e4
$B wait 2000
$B screenshot login.png
# ...继续测试其他流程...
```

### 验证表单
```bash
$B goto https://staging.myapp.com/form
$B snapshot -i
$B fill @e1 "invalid email"     # 错误的邮箱
$B click @e2                    # 提交
$B text @e3                     # 检查错误消息
```

### 链式操作（性能最佳）
```bash
$B chain <<'EOF'
[
  {"command": "goto", "url": "https://staging.myapp.com"},
  {"command": "click", "selector": "@e1"},
  {"command": "screenshot", "path": "result.png"}
]
EOF
```

---

## 9. 最佳实践

1. **总是 snapshot 在交互前**
   ```bash
   $B snapshot -i  # 查看可点击的内容
   ```

2. **使用 @refs 而不是 CSS 选择器**
   ```bash
   # ✗ $B click "button.submit"
   # ✓ $B click @e1
   ```

3. **验证操作成功**
   ```bash
   $B snapshot -D          # 基线
   $B click @e1
   $B snapshot -D          # 对比
   ```

4. **批量命令优先**
   ```bash
   # 用 chain 代替 5 个单独的命令
   ```

5. **导入 cookies 用于认证测试**
   ```bash
   /setup-browser-cookies
   # 现在 /qa 可以访问已登录页面
   ```

6. **按流程阶段使用 skills**
   ```
   规划 → 设计 → 实现 → 审查 → QA → 发布
   /plan  /design  Claude  /review /qa  /ship
   ```

---

## 10. 文件位置速查

| 文件 | 位置 | 用途 |
|------|------|------|
| 状态文件 | `.gstack/browse.json` | 服务器 PID、端口、token |
| Console 日志 | `.gstack/console.log` | JavaScript 错误 |
| Network 日志 | `.gstack/network.log` | HTTP 请求 |
| Dialog 日志 | `.gstack/dialog.log` | alert/confirm 事件 |
| 配置 | `~/.gstack/config.yaml` | 全局设置 |
| Cookies | `~/.gstack/cookies.json` | 导入的 cookies |
| 会话文件 | `~/.gstack/sessions/` | 活跃会话追踪 |

---

## 11. 深入阅读

- **架构**：ARCHITECTURE.md
- **浏览器命令**：BROWSER.md
- **Skill 深潜**：docs/skills.md
- **完整指南**：README.md
- **项目分析**：项目分析.md
- **Browse 详解**：Browse模块详解.md
- **使用指南**：使用指南.md

---

## 12. 获取帮助

- **/help**：CodeBuddy Code 帮助
- **gstack-update-check**：检查升级
- **bun run skill:check**：验证 skills
- **~/.claude/skills/gstack/CONTRIBUTING.md**：贡献指南

---

## 总结速记

| 需要 | 使用 |
|------|------|
| 功能规划 | /plan-ceo-review |
| 架构设计 | /plan-eng-review |
| 设计审计 | /plan-design-review |
| 代码审查 | /review |
| QA 测试 | /qa <url> |
| 发布 PR | /ship |
| 浏览器控制 | $B <command> |
| 每周回顾 | /retro |
| 文档更新 | /document-release |
| 导入 Cookies | /setup-browser-cookies |

**核心哲学**：一个人 + gstack = 20 人团队的产出
