# Gstack 使用指南

## 1. 快速开始（10 分钟）

### 1.1 安装（30 秒）

**前置要求：**
- Claude Code（Anthropic 官方版本）
- Git
- Bun v1.0+ （从 https://bun.sh 安装）

**安装步骤：**

```bash
# 方式 1：个人用户安装
git clone https://github.com/garrytan/gstack.git ~/.claude/skills/gstack
cd ~/.claude/skills/gstack
./setup

# 方式 2：项目级安装（团队共享）
cd /path/to/your/project
cp -Rf ~/.claude/skills/gstack .claude/skills/gstack
rm -rf .claude/skills/gstack/.git
cd .claude/skills/gstack
./setup
```

### 1.2 配置 CLAUDE.md

在你的项目的 `CLAUDE.md` 中添加以下部分：

```markdown
## gstack

使用 /browse skill 处理所有网页浏览。禁止使用 mcp__claude-in-chrome__* 工具。

可用 skills：
- /plan-ceo-review：CEO 模式规划
- /plan-eng-review：工程经理审查
- /plan-design-review：设计师审计
- /design-consultation：设计系统构建
- /review：代码审查
- /ship：发布工程
- /browse：浏览器控制
- /qa：QA 测试和修复
- /qa-only：仅 QA 报告
- /qa-design-review：设计 QA
- /setup-browser-cookies：导入浏览器 cookies
- /retro：每周回顾
- /document-release：文档更新

如果 gstack skills 不工作，运行：`cd .claude/skills/gstack && ./setup`
```

### 1.3 验证安装

```bash
# 检查 skills 是否注册
cd ~/.claude/skills/gstack
bun run skill:check

# 或从项目目录
cd .claude/skills/gstack
bun run skill:check
```

---

## 2. 核心工作流程

### 2.1 完整特性开发周期

**场景**：为电商应用添加"卖家照片上传"功能

#### 第 1 步：CEO 审查 - 重新定义问题

```
你：     我想为卖家添加照片上传功能。
你：     /plan-ceo-review
```

**Claude 输出：**
```
"照片上传"不是真正的功能。真正的问题是帮助卖家创建能卖出去的列表。

如果我们：
A) 自动识别产品 + 拉取规格和竞争对手信息 + 起草列表？（10 星级功能）
B) 添加照片美化 + 自动裁剪？（6 星级功能）
C) 支持批量上传 + CSV 导入？（7 星级功能）
D) 保持简单：仅照片上传（3 星级功能）
...

建议：选择 A 作为核心功能，将 B 和 C 加入待办。
```

**你的决定**：批准方案 A

#### 第 2 步：设计审查 - 检查 AI Slop

```
你：     /plan-design-review
```

**Claude 输出：**
```
┌─────────────────────────────┐
│  设计评分：B  |  AI Slop：C  │
└─────────────────────────────┘

问题：
1. 上传流看起来像默认 Bootstrap 表单（AI Slop）
2. 产品卡片使用统一的圆角半径（AI Slop）
3. 图标网格缺乏视觉层级

推荐设计系统元素：
- 上传区：拖放交互 + 进度指示
- 产品卡片：可变间距、条件圆角
- 图标：仅在必要时显示

详见 DESIGN.md
```

#### 第 3 步：工程审查 - 锁定架构

```
你：     /plan-eng-review
```

**Claude 输出：**
```
建议的数据流：

┌─────────┐    ┌──────────┐    ┌──────────┐    ┌────────┐
│ 上传    │───→│ 分类     │───→│ 充实     │───→│ 草稿   │
│ (sync)  │    │ (async)  │    │ (async)  │    │(async) │
└─────────┘    └──────────┘    └──────────┘    └────────┘

错误处理：
- 上传失败 → 重试 3 次 + 回退
- 分类超时 → 人工审查队列
- 充实 API 失败 → 缓存最后已知状态

测试矩阵（14 个用例）：
□ 单个文件上传
□ 多文件上传
□ 超大文件（100MB+）
□ 网络中断恢复
□ 并发上传
...

安全问题：
1. S3 临时凭证过期处理
2. 大型文件上传的中间人攻击
3. 分类模型投毒防护
```

#### 第 4 步：编码 - Claude 实现功能

```
你：     /implement
Claude：  编写 2,400 行代码：
         - models/product.ts
         - services/classification.ts
         - controllers/upload.ts
         - views/upload-form.tsx
         - migrations/add_product_fields.sql
         - tests/upload.test.ts
         - 等等...
         (~8 分钟)
```

#### 第 5 步：代码审查 - 找出隐藏的 bug

```
你：     /review
```

**Claude 输出：**
```
┌──────────────────────────────┐
│ 发现 3 个问题，2 个已自动修复  │
└──────────────────────────────┘

[AUTO-FIXED] S3 临时凭证过期时孤立文件清理
  修复：添加缩期任务，每小时运行一次清理

[AUTO-FIXED] uploads.status 缺少数据库索引
  修复：添加迁移创建索引

[ASK] 英雄图像选择时的竞争条件
  两个并发请求可能更新到不同的图像
  → 你：是的，需要修复

已修复。所有枚举值已追踪到 switch 语句。
```

#### 第 6 步：QA 测试 - 在真实浏览器中验证

```
你：     /qa https://staging.myapp.com
```

**Claude 输出：**
```
┌──────────────────────────────┐
│ 健康评分：从 D 提升到 A       │
└──────────────────────────────┘

✓ 完整路径：上传 → 分类 → 充实 → 草稿 (E2E)
✓ 移动设备 (iPhone 12)：流畅
✓ 慢网络 (3G 模拟)：正常降级
✓ 坏图像 (1px.jpg)：优雅处理
✗ 批量上传 10 张：只处理 9 张（BUG）

[FIX] 批量上传：数组索引边界错误
  提交：ea3f2a
  重新验证：✓ 正常工作

生成的回归测试已添加。
```

#### 第 7 步：发布 - 自动推送 PR

```
你：     /ship
```

**Claude 输出：**
```
✓ 同步到 main
✓ 运行测试：42 → 51 (+9 新增)
✓ 覆盖率审计：14/14 代码路径 (100%)
✓ 推送到远程
✓ PR 已打开：github.com/you/app/pull/42

PR 标题：feat(upload): 自动产品列表生成
PR 描述：
- 上传照片 + 自动分类
- 从网络充实产品信息
- 智能草稿列表
- 9 个新测试 + 100% 覆盖率
```

---

## 3. 各 Skill 详细指南

### 3.1 `/plan-ceo-review` - CEO 模式

**何时使用**：
- 新功能想法阶段
- 需要重新思考问题
- 确定真正的用户价值

**工作模式**：
```
1. 分析你的功能请求
2. 确定问题的 10 星级版本
3. 提出 8+ 个扩展建议
4. 你挑选 5 个 + 延迟 3 个到积压
5. 生成详细的 PRD
```

**示例调用**：
```
你：我想添加黑暗模式。
你：/plan-ceo-review

Claude 将回答：
"黑暗模式"不是功能。真正的工作是...
- 自动检测用户偏好（系统设置）
- 与现有主题系统集成
- 性能优化（预加载颜色）
...

建议方案及其完整性评分。
```

**输出**：
- 重新定义的问题陈述
- 8+ 个扩展建议
- 完整性评分（10 分制）
- PRD 或详细范围

---

### 3.2 `/plan-eng-review` - 工程经理

**何时使用**：
- 特性已批准，需要架构
- 需要 ASCII 图表和数据流
- 需要完整的测试矩阵

**工作模式**：
```
1. 分析功能要求
2. 设计数据流 + ASCII 图表
3. 识别错误路径
4. 生成测试矩阵
5. 标记安全问题
```

**示例输出**：
```
架构：

┌────────┐     ┌──────┐     ┌──────┐
│ Client │────▶│ API  │────▶│ DB   │
└────────┘     └──────┘     └──────┘
                   │
                   ▼
              ┌─────────┐
              │ Cache   │
              └─────────┘

错误处理：
- 网络超时 → 重试 3 次
- 数据库锁定 → 指数退避
- API 限速 → 入队重试

14 个测试用例：
□ 快路径
□ 慢路径
□ 错误条件 × 3
□ 边界情况 × 8
```

**输出**：
- 完整架构图
- 数据流 (sync/async)
- 错误处理策略
- 14+ 个测试用例
- 安全问题清单
- 性能考虑

---

### 3.3 `/plan-design-review` - 设计审计

**何时使用**：
- 检查设计是否"AI Slop"
- 需要设计系统推荐
- 只想看报告，不修改代码

**工作模式**：
```
1. 审计所有可见组件（80 项检查）
2. 评分设计（A-F）
3. 评分 AI Slop（A-F）
4. 推荐设计系统元素
5. 生成 DESIGN.md
```

**AI Slop 检测**：
- ✗ 默认 Bootstrap 表单
- ✗ 渐变英雄部分
- ✗ 图标网格（无变化）
- ✗ 统一的圆角
- ✗ 不必要的玻璃态效果

**输出**：
- 设计评分 + AI Slop 评分
- 80 项审计结果
- DESIGN.md（推荐元素）
- 前后对比（仅报告）

---

### 3.4 `/design-consultation` - 设计伙伴

**何时使用**：
- 从零开始设计系统
- 需要创意风险 + 安全选项
- 需要真实产品模型

**工作模式**：
```
1. 研究竞争设计
2. 提出安全 + 创意选项
3. 生成设计模型
4. 编写 DESIGN.md
5. 其他 skills 读取它
```

**输出**：
- 竞争分析
- 设计原则 + 调色板
- 完整设计系统文档
- 真实产品模型截图
- Figma 原型链接（如果适用）

---

### 3.5 `/review` - 代码审查

**何时使用**：
- PR 在登陆前
- 需要找出生产 bug
- 需要验证完整性

**检查内容**：
- SQL 注入、竞争条件
- 错误处理的缺失
- 枚举值追踪（所有 switch/cases）
- 边界条件
- 安全问题

**工作模式**：
```
1. 生成 diff
2. 逐行扫描问题
3. 自动修复明显的 bug
4. 标记需要确认的问题
5. 追踪评审状态
```

**输出示例**：
```
┌──────────────────────────────┐
│ 发现 5 个问题，2 个已自动修复  │
└──────────────────────────────┘

[AUTO-FIXED] users.status 缺少数据库索引
[AUTO-FIXED] 错误的 HTTP 状态码 (500 vs 400)
[ASK] 重复处理竞争条件?
[ASK] locale 参数验证缺失
[FLAG] 第三方 API 新端点未在 CONTRIBUTING.md 中记录
```

---

### 3.6 `/ship` - 发布工程

**何时使用**：
- 准备合并 PR
- 需要运行完整的发布清单
- 自动化 CI/CD

**工作模式**：
```
1. 同步到 main
2. 运行测试套件
3. 审计覆盖率
4. 检查 PR 就绪性
5. 推送并打开 PR
```

**发布前清单**：
- ✓ 所有测试通过
- ✓ 覆盖率 ≥ 80%（或更高）
- ✓ 无 linter 错误
- ✓ PR 标题和描述完整
- ✓ CHANGELOG 已更新
- ✓ 文档已更新

**输出**：
```
✓ 测试：42 → 51 (+9 新增)
✓ 覆盖率：14/14 代码路径 (100%)
✓ PR 链接：github.com/you/app/pull/42
```

---

### 3.7 `/qa` - QA 测试和修复

**何时使用**：
- 在登陆前测试 staging URL
- 需要找到并修复 bug
- 需要生成回归测试

**三个等级**：

| 等级 | 覆盖范围 | 用途 |
|------|--------|------|
| **Quick** | 严重 + 高优先级 | 快速冒烟测试 |
| **Standard** | + 中等优先级 | 标准 QA 周期 |
| **Exhaustive** | + 化妆品 bug | 完整审计 |

**工作模式**：
```
1. 打开真实浏览器
2. 登录应用
3. 遍历关键流程
4. 记录 bug
5. 为每个 bug 创建修复提交
6. 重新验证修复
7. 生成回归测试
```

**输出**：
```
┌────────────────────────────────┐
│ 健康评分：从 D 提升到 A (9 bug 修复) │
└────────────────────────────────┘

✓ 完整用户流
✓ 移动设备 (iPhone 12)
✓ 慢网络 (3G)
✓ 表单验证边界
✗ 批量操作 - 某些项跳过 (BUG - 已修复)
✗ 刷新后丢失状态 (BUG - 已修复)

提交：ea3f2a, eb4d1b, ec5e2c
回归测试已生成并集成。
```

**回归测试示例**：
```typescript
test('批量操作处理所有项', () => {
  // 遍历 10 个项
  // 验证所有都被处理
})

test('刷新后状态持续', () => {
  // 执行操作
  // F5 刷新
  // 验证状态
})
```

---

### 3.8 `/qa-only` - 仅 QA 报告

**何时使用**：
- 只想要 bug 报告
- 不希望 Claude 修改代码
- 手动审视每个修复

**与 `/qa` 的区别**：
- `/qa`：查找 bug + 自动修复
- `/qa-only`：查找 bug + 仅报告

**输出格式**：
```markdown
# QA 报告

## 严重 bug (必须修复)
1. 缺失输入验证 → 防止 XSS 攻击
2. 竞争条件 → 数据丢失

## 中等 bug (应该修复)
3. 性能：图像加载慢
4. 样式：移动设备溢出

## 化妆品 (可以修复)
5. 拼写错误：'Adress' 应为 'Address'
```

---

### 3.9 `/browse` - 浏览器控制

**何时使用**：
- 需要在网页上执行操作
- 截图验证
- 从网页提取信息

**基本命令**：

```bash
# 导航
$B goto https://example.com
$B back
$B reload

# 交互
$B snapshot                    # 查看可点击的元素
$B click @e1                   # 点击元素
$B fill @e2 "search term"     # 填充输入框
$B press Enter                 # 按键

# 验证
$B screenshot output.png       # 截图
$B text @e3                    # 获取文本
$B html @e4                    # 获取 HTML

# 调试
$B console                     # JavaScript 错误
$B network                     # 网络请求
```

**典型流程**：
```
$B goto https://staging.myapp.com
$B snapshot -i                 # 查看可交互元素
$B click @e1                   # 点击"登录"
$B fill @e2 "user@example.com" # 输入邮箱
$B fill @e3 "password"         # 输入密码
$B click @e4                   # 点击"登录"按钮
$B wait 2000                   # 等待导航
$B screenshot dashboard.png    # 验证登录成功
```

---

### 3.10 `/setup-browser-cookies` - Cookie 导入

**何时使用**：
- 需要在 QA 中测试已认证的页面
- 想避免重新登录

**工作流程**：
```
你：    /setup-browser-cookies

Claude：检测你的浏览器（Chrome、Arc、Edge）
       要求 Keychain 访问权限
       [你批准]
       导入 cookies
       验证登录

现在 /qa 和 /browse 都可以访问已认证的页面
```

---

### 3.11 `/retro` - 每周回顾

**何时使用**：
- 每周工作总结
- 追踪发货速度
- 识别趋势

**输出示例**：
```
┌─────────────────────────────┐
│ 每周回顾 (Mar 12-18, 2026)  │
└─────────────────────────────┘

发货时间表
━━━━━━━━━━━━━━━━━━━━━━━━━━
Mon: ████████░░ 8 个提交
Tue: ██████░░░░ 6 个提交
Wed: ███████████ 11 个提交
Thu: █████░░░░░ 5 个提交
Fri: ██████░░░░ 6 个提交

统计
━━━━━━━━━━━━━━━━━━━━━━━━━━
提交：36
代码行：+4,200 -1,100 (net: +3,100)
PR：5 个
bug 修复：8 个
新功能：3 个
测试：+45 个

发货连胜：12 天 ✓
测试健康：89% 通过率 ↑ 3%
代码覆盖率：82% ↑ 1%
```

---

### 3.12 `/document-release` - 文档更新

**何时使用**：
- 发布新功能后
- 自动更新陈旧的文档
- 保持 README、API 文档同步

**工作模式**：
```
1. 读取项目中的所有 .md 文件
2. 交叉引用最新的 git diff
3. 识别陈旧的部分
4. 自动更新并提交
```

**更新示例**：
```
README.md：
- ❌ "支持 12 个语言" → ✓ "支持 15 个语言"（新增中文、日语、韩语）

API.md：
- ❌ `POST /users/upload` → ✓ 添加了新的 `batch` 参数文档

ARCHITECTURE.md：
- ✓ 已是最新（无更改）

CHANGELOG.md：
- ❌ 缺失 v0.3.3 → ✓ 已添加发布说明
```

---

## 4. 日常使用场景

### 4.1 场景 1：构建新的 Web 功能

```bash
# 1. 规划阶段
/plan-ceo-review        # 重新思考问题
/plan-design-review     # 检查设计
/plan-eng-review        # 锁定架构

# 2. 实现阶段（Claude 编码）
# ...编写代码...

# 3. 质量阶段
/review                 # 代码审查
/qa <staging-url>       # QA 测试

# 4. 发布阶段
/ship                   # 打开 PR
/document-release       # 更新文档
```

### 4.2 场景 2：修复生产 bug

```bash
# 1. 识别
/qa <production-url>    # 查找 bug

# 2. 修复
/review                 # 审查 bug 修复

# 3. 验证
/qa <staging-url>       # 回归测试

# 4. 发布
/ship                   # 发布修复
```

### 4.3 场景 3：重构现有功能

```bash
# 1. 理解
/plan-eng-review        # 当前架构
/browse <staging-url>   # 交互测试

# 2. 规划
/plan-ceo-review        # 重构目标
/plan-design-review     # 用户体验更改

# 3. 重构
# ... Claude 编码 ...

# 4. 验证
/qa <staging-url>       # 完整回归
/review                 # 代码质量
```

---

## 5. 提示和最佳实践

### 5.1 使用 Refs 而非 CSS 选择器

**✗ 不好**：
```bash
$B click "div.btn.btn-primary:nth-child(3)"
```

**✓ 好**：
```bash
$B snapshot -i              # 获取可交互元素
$B click @e3                # 使用 ref
```

### 5.2 使用快照来了解页面状态

```bash
# 在每个重要步骤后快照
$B snapshot -i              # 交互元素
$B snapshot -D              # 对比（验证变化）
$B screenshot output.png    # 可视验证
```

### 5.3 使用链式命令减少往返

```bash
# ✗ 5 个单独的命令 = 5 个 HTTP 往返
$B goto https://example.com
$B fill @e1 "input"
$B click @e2
$B wait 1000
$B screenshot

# ✓ 1 个链式命令
$B chain <<'EOF'
[
  {"command": "goto", "url": "https://example.com"},
  {"command": "fill", "selector": "@e1", "text": "input"},
  {"command": "click", "selector": "@e2"},
  {"command": "wait", "ms": 1000},
  {"command": "screenshot"}
]
EOF
```

### 5.4 导入浏览器 cookies 进行认证测试

```bash
# 一次性导入
/setup-browser-cookies

# 现在 /qa 和 /browse 可以访问已认证的页面
/qa https://staging.myapp.com
```

### 5.5 使用 -D 标志验证操作成功

```bash
$B snapshot -D              # 建立基线
$B click @e3                # 执行操作
$B snapshot -D              # 对比
# 输出显示变化 ✓
```

### 5.6 注释快照进行调试

```bash
# 在屏幕上显示所有 @refs
$B snapshot -a -o debug.png

# 查看 debug.png 了解 refs 的位置
open debug.png
```

---

## 6. 故障排除

### 问题 1：Skills 不显示

**症状**：在 Claude Code 中找不到 `/browse`、`/review` 等

**解决方案**：
```bash
cd ~/.claude/skills/gstack
./setup

# 或如果在项目中
cd .claude/skills/gstack
./setup
```

### 问题 2：Browse 命令超时

**症状**：`$B goto https://example.com` 超时

**解决方案**：
```bash
# 重建二进制
cd ~/.claude/skills/gstack
bun run build

# 检查 Chromium 是否在运行
ps aux | grep chrome

# 手动重启服务器
rm -f .gstack/browse.json
# 下一个命令将重启
```

### 问题 3：Cookies 导入失败

**症状**：`/setup-browser-cookies` 说"浏览器未找到"

**解决方案**：
- 确保 Chrome、Arc、Edge 或 Brave 已安装
- 手动导出 cookies（JSON 格式）并使用 `$B cookie-import <file.json>`

### 问题 4：Ref 过期

**症状**：`Ref @e3 已过期 - 元素不再存在`

**解决方案**：
```bash
# 获取新 refs
$B snapshot

# 从新 refs 中选择元素
$B click @e5  # 不同的 ref，因为页面变化了
```

---

## 7. 性能优化

### 7.1 减少浏览器启动

- 第一次启动：~3 秒
- 30 分钟后自动关闭
- **技巧**：保持你的 Claude 会话打开

### 7.2 批量操作

```bash
# ✗ 10 个单独的命令 = 2-3 秒/命令 = 20-30 秒
for i in 1..10; do
  $B click @e$i
  $B wait 500
done

# ✓ 1 个链式操作 = 1-2 秒
$B chain <<'EOF'
[
  {"command": "click", "selector": "@e1"},
  {"command": "wait", "ms": 500},
  {"command": "click", "selector": "@e2"},
  ...
]
EOF
```

### 7.3 并行工作

- 使用 **Conductor** 运行 10 个 Claude Code 会话
- 每个会话有自己的浏览器实例
- 完全独立的并行 QA/开发

---

## 8. 进阶用法

### 8.1 自定义评估

```bash
# 运行评估套件
EVALS=1 bun test test/skill-e2e.test.ts

# 查看结果
eval:watch
```

### 8.2 贡献者模式

```bash
# 启用贡献者日志
~/.claude/skills/gstack/bin/gstack-config set gstack_contributor true

# 修改 skills 并测试
bun run dev:skill

# 查看自动诊断日志
cat ~/.gstack/contributor-logs/
```

### 8.3 自动升级

```bash
# 启用自动升级检查
~/.claude/skills/gstack/bin/gstack-config set auto_upgrade true

# 或手动升级
/gstack-upgrade
```

---

## 总结

**Gstack 使用的黄金法则：**

1. **规划第一**：使用 `/plan-*` skills 在编码前确认方向
2. **设计审计**：使用 `/plan-design-review` 避免 AI Slop
3. **完整性**：使用"沸湖"原则 - 完整实现，边际成本为零
4. **自动化测试**：使用 `/qa` 获得真实浏览器验证
5. **持续审查**：使用 `/review` 找出隐藏的 bug
6. **文档同步**：使用 `/document-release` 保持一致

**典型一天流程（使用 Conductor）：**

```
10:00 - 早上计划     /plan-ceo-review + /plan-eng-review (2 分钟)
10:30 - 开发会话 1   /implement 新功能 (30 分钟)
11:00 - 开发会话 2   修复现存 bug (20 分钟)
11:20 - QA 会话      /qa <staging> (15 分钟)
11:35 - 审查会话     /review 所有分支 (5 分钟)
11:40 - 发布会话     /ship 就绪分支 (5 分钟)
12:00 - 团队回顾     /retro 本周统计 (5 分钟)
```

一个人，10 个并行代理，每个都有正确的认知模式。这就是 gstack 的价值。
