# Gstack 项目全面分析

## 项目概览

**Gstack** 是一个由 Y Combinator CEO Garry Tan 开发的开源 AI 工程平台，目标是将 Claude Code 转变为完整的虚拟工程团队。这个项目通过一系列斜杠命令（slash commands）将复杂的软件开发工作流程自动化，使个人开发者能够像一个 20 人团队一样高效地工作。

**核心统计：**
- 版本：0.3.3
- 开源协议：MIT（完全免费）
- 许可证：MIT License
- 语言：TypeScript + Bash
- 技术栈：Bun + Playwright + Chromium

---

## 一、项目使命和价值主张

### 目标用户
- **创始人和 CEO**（尤其是技术背景的）：使用 AI 像 20 人团队一样快速开发
- **Claude Code 初学者**：提供结构化的角色而不是空白提示
- **Tech Lead 和资深工程师**：为每次 PR 引入严格的评审、QA 和自动化发布流程

### 核心成就
作者在 60 天内完成了以下工作：
- 编写 **600,000+ 行生产代码**（35% 为测试）
- **日均 10,000-20,000 行可用代码**（兼职工作，同时担任 YC 总裁）
- 2026 年 GitHub 贡献数：1,237+（与 2013 年的 772 相比有显著提升）

---

## 二、核心架构

### 2.1 整体架构设计

```
Claude Code                     gstack
─────────────────            ──────────
                             ┌──────────────────────┐
  Tool call: $B snapshot      │  CLI (compiled binary)│
  ──────────────────────→     │  • reads state file   │
                             │  • POST /command      │
                             │    to localhost:PORT   │
                             └──────────┬───────────┘
                                        │ HTTP
                             ┌──────────▼───────────┐
                             │  Server (Bun.serve)   │
                             │  • dispatches command  │
                             │  • talks to Chromium   │
                             │  • returns plain text  │
                             └──────────┬───────────┘
                                        │ CDP
                             ┌──────────▼───────────┐
                             │  Chromium (headless)   │
                             │  • persistent tabs     │
                             │  • cookies carry over  │
                             │  • 30min idle timeout  │
                             └───────────────────────┘
```

**关键设计决策：**
- **第一次调用**：启动浏览器约 3 秒
- **后续调用**：约 100-200 毫秒（HTTP POST 往返）
- **持久性状态**：cookies、localStorage、登录会话在命令间保留
- **自动生命周期**：30 分钟空闲后自动关闭

### 2.2 为什么选择 Bun

1. **编译二进制**：`bun build --compile` 生成单一的 ~58MB 可执行文件，无需运行时配置
2. **原生 SQLite**：内置 `new Database()` API，用于解密 Chromium 的 cookie 数据库
3. **原生 TypeScript**：开发时无需编译步骤，直接运行 `.ts` 文件
4. **内置 HTTP 服务器**：`Bun.serve()` 简洁高效，避免了 Express/Fastify 的开销

### 2.3 状态文件管理

**位置**：`.gstack/browse.json`（权限：0o600，仅所有者可读）

```json
{
  "pid": 12345,
  "port": 34567,
  "token": "uuid-v4",
  "startedAt": "...",
  "binaryVersion": "abc123"
}
```

**特性：**
- 随机端口选择（10000-60000），支持 10 个 Conductor 工作区并行运行
- 原子写入（tmp + rename）防止损坏
- 二进制版本自动重启：若二进制版本不匹配，自动重启服务器

---

## 三、核心模块详解

### 3.1 Browse 模块（浏览器自动化）

**位置**：`browse/` 目录

**代码规模**：
- `src/` 下 TypeScript 代码：4,098 行
- 编译后二进制大小：~58MB

**主要子模块：**

| 文件 | 功能 | 行数 |
|------|------|------|
| `cli.ts` | CLI 客户端 - 读取状态文件、发送 HTTP 请求、输出结果 | ~200 |
| `server.ts` | Bun HTTP 服务器 - 路由命令、调用 Playwright | ~300 |
| `browser-manager.ts` | Chromium 生命周期管理 - 启动、标签页、refs 映射、崩溃处理 | ~600 |
| `snapshot.ts` | 可访问性树解析 - @ref 分配、Locator 映射、diff/注释/光标交互 | ~800 |
| `read-commands.ts` | 读取命令 - text、html、links、js、css、is、dialog 等 | ~600 |
| `write-commands.ts` | 写入命令 - click、fill、select、upload、dialog-accept 等 | ~700 |
| `meta-commands.ts` | 服务器管理命令 - chain、diff、snapshot 路由 | ~400 |
| `cookie-import-browser.ts` | 浏览器 Cookie 解密和导入 | ~300 |
| `buffers.ts` | 三个环形缓冲区 - console、network、dialog 日志 | ~150 |

**核心命令分类：**

1. **导航命令**：`goto`、`back`、`forward`、`reload`、`url`
2. **读取命令**（无副作用）：`text`、`html`、`links`、`forms`、`accessibility`、`console`、`cookies`、`storage`、`perf`
3. **快照命令**：`snapshot [-i] [-c] [-d N] [-s sel] [-D] [-a] [-o] [-C]`
4. **交互命令**（有副作用）：`click`、`fill`、`select`、`hover`、`type`、`press`、`scroll`、`wait`、`viewport`、`upload`
5. **检查命令**：`js`、`eval`、`css`、`attrs`、`is`、`dialog`、`network`
6. **可视命令**：`screenshot`、`pdf`、`responsive`
7. **对比命令**：`diff <url1> <url2>`
8. **多标签页**：`tabs`、`tab`、`newtab`、`closetab`
9. **Cookie 管理**：`cookie-import`、`cookie-import-browser`
10. **批量操作**：`chain`（JSON 从 stdin）

### 3.2 Ref 系统（元素寻址）

**核心创新**：无需 CSS 选择器或 XPath 的元素寻址

**工作流程：**

```
1. 执行 snapshot      → 获取可访问性树
2. 分配 refs          → @e1, @e2, @e3...
3. 构建 Locator       → getByRole(role, { name }).nth(index)
4. 存储映射          → BrowserManager 实例

后续：
5. 执行 click @e3     → 查询 @e3 → Locator → locator.click()
```

**优势：**
- ✅ 避免 DOM 变更（不触发 CSP 错误）
- ✅ 框架友好（React/Vue hydration 无影响）
- ✅ 支持 Shadow DOM
- ✅ 快速检测过时 refs（5ms 开销）

**特殊 refs：**
- `@e1, @e2...`：标准 ARIA 元素
- `@c1, @c2...`：光标可交互元素（`cursor:pointer`、`onclick`、`tabindex`）

### 3.3 日志架构

**三个环形缓冲区**（各 50,000 条记录）：
- Console 消息
- Network 请求
- Dialog 事件

**特点：**
- O(1) push 操作
- 每秒异步刷新到磁盘
- HTTP 请求处理不受磁盘 I/O 阻塞
- 缓冲区可存活 1 秒数据丢失

---

### 3.4 Security 模块（安全模型）

**三层防御：**

1. **localhost 只绑定**：HTTP 服务器不在 `0.0.0.0` 上暴露
2. **Bearer Token 认证**：每个会话生成随机 UUID，状态文件权限 0o600
3. **Cookie 安全**：
   - Keychain 访问需用户确认（macOS）
   - 解密发生在进程内存中
   - Cookie DB 以只读方式打开
   - 密钥缓存仅限会话期间
   - 日志中不包含 cookie 值

---

## 四、13 个 Skill（斜杠命令）

### 4.1 规划阶段（3 个 skills）

#### 1. `/plan-ceo-review` - CEO/创始人模式
**角色**：CEO / 创始人
**核心功能**：重新思考问题，找到 10 星级产品
**四种模式**：
- **SCOPE EXPANSION**：大胆梦想
- **SELECTIVE EXPANSION**：保持范围 + 精选扩展
- **HOLD SCOPE**：最大严谨性
- **SCOPE REDUCTION**：精简到核心

**输出**：
- 8+ 个扩展建议
- 你可以挑选 5 个，延迟 3 个到积压

**工具使用**：Read、Grep、Glob、Bash、AskUserQuestion

#### 2. `/plan-eng-review` - 工程经理模式
**角色**：工程经理
**核心功能**：锁定架构、数据流、图表、边界情况、测试
**输出**：
- ASCII 架构图
- 数据流/状态机/错误路径
- 14 个测试用例矩阵
- 6 个失败模式映射
- 3 个安全问题识别

#### 3. `/plan-design-review` - 高级设计师模式
**角色**：高级设计师
**核心功能**：80 项设计审计 + AI Slop 检测
**输出**：
- 设计评分（A-F）
- AI Slop 评分
- `DESIGN.md` 文件
- 3 个 AI Slop 模式标记

**特点**：仅报告，不修改代码

---

### 4.2 设计阶段（2 个 skills）

#### 4. `/design-consultation` - 设计合作伙伴
**角色**：设计合作伙伴
**核心功能**：从零开始构建完整设计系统
**输出**：
- 研究现有设计
- 提出安全和创意风险的选择
- 生成真实产品的模型
- 编写 `DESIGN.md`

#### 5. `/design-review` - 会写代码的设计师
**角色**：设计师（可编程）
**核心功能**：运行审计，然后修复发现的问题
**特点**：
- 原子提交
- 修复前后截图
- 与 `/plan-design-review` 相同的审计方法

---

### 4.3 开发阶段（2 个 skills）

#### 6. `/review` - 资深工程师
**角色**：资深工程师（代码审查）
**核心功能**：查找通过 CI 但会在生产中崩溃的 bug
**检查项**：
- SQL 安全性
- LLM 信任边界违规
- 条件副作用
- 完整性缺口
- 每个新枚举值遍历所有 switch 语句

**特点**：
- 自动修复明显问题
- 标记需要确认的问题
- 追踪评审状态

#### 7. `/ship` - 发布工程师
**角色**：发布工程师
**核心功能**：同步 main、运行测试、审计覆盖率、推送、打开 PR
**特点**：
- 如无测试框架则引导安装
- 覆盖率审计
- 引导单条命令发布
- 从零开始引导测试框架

---

### 4.4 QA 阶段（3 个 skills）

#### 8. `/browse` - QA 工程师
**角色**：QA 工程师（浏览器视角）
**核心功能**：给 agent 眼睛 - 真实 Chromium 浏览器、真实点击、真实截图
**性能**：约 100 毫秒/命令

#### 9. `/qa` - QA 经理
**角色**：QA 经理
**核心功能**：测试应用、找 bug、用原子提交修复、重新验证
**特点**：
- 自动生成每个修复的回归测试
- 三个层级：Quick（严重/高），Standard（+中等），Exhaustive（+化妆品）
- 修复前后健康评分
- 发布准备就绪摘要

#### 10. `/qa-only` - QA 报告员
**角色**：QA 报告员
**核心功能**：与 `/qa` 相同的方法论，但仅报告不修改
**使用场景**：想要纯 bug 报告而无代码更改时

---

### 4.5 辅助工具（3 个 skills）

#### 11. `/setup-browser-cookies` - 会话管理器
**角色**：会话管理器
**核心功能**：从实际浏览器导入 cookies
**支持浏览器**：Chrome、Arc、Brave、Edge
**用途**：测试已认证的页面

#### 12. `/retro` - 工程经理（回顾）
**角色**：工程经理
**核心功能**：团队感知的每周回顾
**输出**：
- 人员统计
- 发货连胜记录
- 测试健康趋势
- 增长机会

#### 13. `/document-release` - 技术作者
**角色**：技术作者
**核心功能**：更新所有项目文档以匹配最近发货内容
**特点**：自动捕获陈旧的 README

---

## 五、项目结构

```
gstack/
├── browse/                      # 浏览器自动化核心（4,098 行 TS）
│   ├── src/
│   │   ├── cli.ts              # CLI 客户端
│   │   ├── server.ts           # HTTP 服务器
│   │   ├── browser-manager.ts  # Chromium 生命周期
│   │   ├── snapshot.ts         # @ref 系统
│   │   ├── read-commands.ts    # 读取命令
│   │   ├── write-commands.ts   # 写入命令
│   │   ├── meta-commands.ts    # 元命令
│   │   ├── cookie-import-browser.ts  # Cookie 管理
│   │   ├── buffers.ts          # 日志缓冲区
│   │   └── ...
│   ├── test/                   # 集成测试 + HTML fixtures
│   └── dist/                   # 编译二进制 (~58MB)
│
├── plan-ceo-review/            # CEO 模式规划 skill
├── plan-eng-review/            # 工程经理规划 skill
├── plan-design-review/         # 设计师审计 skill
├── design-consultation/        # 设计系统构建 skill
├── design-review/              # 代码设计修复 skill
├── review/                     # 代码审查 skill
├── ship/                       # 发布工程 skill
├── qa/                         # QA 测试和修复 skill
├── qa-only/                    # QA 报告 skill
├── setup-browser-cookies/      # Cookie 导入 skill
├── retro/                      # 每周回顾 skill
├── document-release/           # 文档更新 skill
│
├── bin/                        # 工具脚本
│   ├── dev-setup              # 开发环境设置
│   ├── dev-teardown           # 开发环境清理
│   ├── gstack-config          # 配置管理
│   ├── gstack-diff-scope      # diff 范围检查
│   ├── gstack-slug            # URL 生成
│   ├── gstack-update-check    # 升级检查
│
├── scripts/                    # 构建脚本
│   ├── gen-skill-docs.ts      # 从代码生成 SKILL.md
│   ├── eval-compare.ts        # 对比评估
│   ├── eval-list.ts           # 列表评估
│   ├── eval-summary.ts        # 评估摘要
│   ├── skill-check.ts         # Skill 验证
│   └── ...
│
├── test/                       # 测试套件
│   ├── helpers/               # 测试工具
│   │   ├── eval-store.ts      # 评估存储
│   │   ├── session-runner.ts  # 会话运行器
│   │   ├── llm-judge.ts       # LLM 评判
│   │   └── ...
│   ├── fixtures/              # 测试数据
│   ├── skill-e2e.test.ts      # E2E 测试
│   └── skill-llm-eval.test.ts # LLM 评估测试
│
├── ARCHITECTURE.md             # 架构文档（详细）
├── BROWSER.md                 # 浏览器命令参考
├── README.md                  # 项目主文档
├── package.json               # 项目配置
└── VERSION                    # 版本号
```

---

## 六、技术栈详解

### 6.1 核心依赖

```json
{
  "dependencies": {
    "playwright": "^1.58.2",    // 浏览器自动化
    "diff": "^7.0.0"             // 文本对比
  },
  "devDependencies": {
    "@anthropic-ai/sdk": "^0.78.0"  // Claude API
  },
  "engines": {
    "bun": ">=1.0.0"            // Bun 运行时
  }
}
```

### 6.2 构建命令

```bash
# 构建编译二进制
bun run build

# 生成 SKILL 文档
bun run gen:skill-docs

# 开发模式
bun run dev

# 启动服务器
bun run server

# 运行测试
bun test browse/test/ test/

# 运行评估
EVALS=1 bun test test/skill-llm-eval.test.ts test/skill-e2e.test.ts

# Skill 检查
bun run skill:check
```

---

## 七、使用方式

### 7.1 安装（30 秒）

**要求**：Claude Code、Git、Bun v1.0+

**方式 1：个人机器安装**

```bash
git clone https://github.com/garrytan/gstack.git ~/.claude/skills/gstack
cd ~/.claude/skills/gstack
./setup
```

然后在 `CLAUDE.md` 中添加 gstack 部分，配置使用 `/browse` skill 进行所有网页浏览。

**方式 2：项目级安装（团队共享）**

```bash
cp -Rf ~/.claude/skills/gstack .claude/skills/gstack
rm -rf .claude/skills/gstack/.git
cd .claude/skills/gstack
./setup
```

在项目 `CLAUDE.md` 中添加 gstack 配置。

### 7.2 快速开始（10 分钟）

```
1. 安装 gstack（30 秒）
2. 运行 /plan-ceo-review on any feature idea
3. 运行 /review on any branch with changes
4. 运行 /qa on your staging URL
5. 停止。你会知道这是否适合你。
```

### 7.3 典型工作流程

**完整功能开发周期：**

```
你：      我想为卖家添加照片上传功能。
你：      /plan-ceo-review
Claude：  "照片上传"不是功能。真正的工作是帮助卖家创建能卖出去的列表。
         如果我们自动识别产品、从网络提取规格和竞争对手、自动起草列表呢？
         那是 10 星。"上传照片"是 3 星。我们建造哪一个？
         [8 个扩展建议，你挑选 5 个，延迟 3 个]

你：      /plan-design-review
Claude：  设计评分：B | AI Slop 评分：C
         "上传流看起来像默认 Bootstrap 表单。"
         [80 项审计，推断你的设计系统，导出 DESIGN.md]
         [标记 3 个 AI Slop 模式：渐变英雄、图标网格、统一半径]

你：      /plan-eng-review
Claude：  ┌─────────┐    ┌──────────┐    ┌──────────┐    ┌─────────┐
         │ 上传    │───▶│ 分类     │───▶│ 充实     │───▶│ 草稿   │
         │ (sync)  │    │ (async)  │    │ (async)  │    │ (async) │
         └─────────┘    └──────────┘    └──────────┘    └─────────┘
         [每个数据流、状态机、错误路径的 ASCII 图]
         [14 个测试用例、6 个失败模式、3 个安全问题]

你：      批准计划。退出计划模式。
Claude：  [在 11 个文件中写 2,400 行 - 模型、服务、控制器、
         视图、迁移、测试。约 8 分钟。]

你：      /review
Claude：  [AUTO-FIXED] 上传失败时的孤立 S3 清理
         [AUTO-FIXED] listings.status 上缺少索引
         [ASK] 英雄图像选择上的竞争条件 → 你：是的
         [追踪每个新枚举值遍历所有 switch 语句]
         3 个问题 - 2 个自动修复，1 个已修复。

你：      /qa https://staging.myapp.com
Claude：  [打开真实浏览器，登录，上传照片，点击流]
         上传 → 分类 → 充实 → 草稿：端到端 ✓
         移动设备：✓ | 慢连接：✓ | 坏图像：✓
         [找到 bug：第二次上传时预览不清除 - 修复]
         生成的回归测试。

你：      /ship
Claude：  测试：42 → 51 (+9 新增)
         覆盖率：14/14 代码路径（100%）
         PR：github.com/you/app/pull/42
```

---

## 八、核心特性和创新

### 8.1 "Boil the Lake"（沸湖原则）

**核心哲学**：AI 使完整性的边际成本接近零。当呈现选项时：

- 如果选项 A 是完整实现（全平价、所有边界情况、100% 覆盖）
- 选项 B 是快捷方式（节省适度工作量）
- **总是推荐 A**

| 任务类型 | 人类团队 | CC+gstack | 压缩比 |
|---------|---------|----------|-------|
| 样板代码/脚手架 | 2 天 | 15 分钟 | ~100x |
| 测试编写 | 1 天 | 15 分钟 | ~50x |
| 功能实现 | 1 周 | 30 分钟 | ~30x |
| Bug 修复 + 回归测试 | 4 小时 | 15 分钟 | ~20x |
| 架构 / 设计 | 2 天 | 4 小时 | ~5x |
| 研究 / 探索 | 1 天 | 3 小时 | ~3x |

### 8.2 持久化浏览器状态

**与每命令启动浏览器的对比：**

| 方面 | 每命令启动 | gstack 模式 |
|------|----------|-----------|
| 启动时间 | 2-3 秒/命令 | 3 秒（首次），100-200ms（后续） |
| 登录会话 | 丢失 | 保留 |
| Cookies | 丢失 | 保留 |
| localStorage | 丢失 | 保留 |
| 打开标签页 | 丢失 | 保留 |
| 20 个命令总时间 | 40-60 秒 | 3 秒 + 2-4 秒 = 5-7 秒 |

### 8.3 ref 系统（无 CSS 选择器）

**传统方法问题：**
- ❌ CSP 阻止 DOM 修改
- ❌ React/Vue 框架冲突
- ❌ Shadow DOM 无法跨越

**gstack ref 解决方案：**
- ✅ 使用可访问性 API（ARIA 树）
- ✅ Playwright Locators（外部于 DOM）
- ✅ 自动过时检测（5ms 开销）
- ✅ 支持 @e（ARIA）和 @c（光标可交互）

### 8.4 智能评审路由

**概念**：CEO 不需要查看基础设施 bug 修复，设计评审不需要查看后端更改。

gstack 追踪运行过的评审，确定什么是合适的，直接执行。

### 8.5 E2E 测试基础设施

**三层评估系统：**

| 层级 | 内容 | 成本 | 速度 |
|------|------|------|------|
| 1 | 静态验证 - 解析命令、验证注册表 | 免费 | <2s |
| 2 | E2E via claude -p - 真实会话 | ~$3.85 | ~20min |
| 3 | LLM-as-judge - Sonnet 评分清晰度/完整性 | ~$0.15 | ~30s |

第 1 层在每个 `bun test` 中运行。第 2 和 3 层需要 `EVALS=1`。

---

## 九、配置和扩展

### 9.1 配置文件

**位置**：`~/.gstack/config.yaml`

**常见配置：**
```yaml
auto_upgrade: true              # 自动升级检查
gstack_contributor: false       # 贡献者模式
```

### 9.2 SKILL.md 模板系统

**工作流程：**
```
SKILL.md.tmpl（手写）
    ↓
gen-skill-docs.ts（从源代码读取元数据）
    ↓
SKILL.md（提交的自动生成文件）
```

**动态占位符：**
- `{{COMMAND_REFERENCE}}`：命令表
- `{{SNAPSHOT_FLAGS}}`：flag 参考
- `{{PREAMBLE}}`：启动块
- `{{BASE_BRANCH_DETECT}}`：动态基分支检测
- `{{TEST_BOOTSTRAP}}`：测试框架检测

---

## 十、局限性和未来方向

### 10.1 有意省略的功能

- ❌ WebSocket 流：HTTP 请求/响应更简单
- ❌ MCP 协议：开销太大
- ❌ 多用户支持：一个服务器 / 一个用户
- ❌ Windows/Linux Cookie 解密：仅 macOS Keychain
- ❌ iframe 支持：最常见的缺失功能

### 10.2 安全考虑

**意图防御：**
- 仅 localhost 绑定
- Bearer token 认证
- Keychain 访问需用户确认
- Cookie 值不出现在日志中
- Shell 注入防止（硬编码路径）

---

## 十一、开发和贡献

### 11.1 开发设置

```bash
# 克隆并进入项目
cd ~/.claude/skills/gstack

# 安装依赖
bun install

# 构建二进制
bun run build

# 运行开发服务器
bun run server

# 运行 CLI（开发模式）
bun run dev
```

### 11.2 测试

```bash
# 单元 + 集成测试
bun test

# E2E 评估（需要 $3.85 + Claude 访问）
EVALS=1 bun test

# 完整评估
EVALS=1 EVALS_ALL=1 bun test
```

### 11.3 Skill 验证

```bash
# 检查所有 skills 有效性
bun run skill:check

# 在开发中测试 skill
bun run dev:skill
```

---

## 十二、许可证和社区

- **许可证**：MIT（完全免费，无限制）
- **原始作者**：Garry Tan（YC 总裁 & CEO）
- **状态**：积极维护和升级
- **目标**：所有人都在这个旅程中

**招聘**：Y Combinator 正在招聘有兴趣每天编写 10K+ LOC 的人才，帮助加强 gstack。

---

## 十三、总结

**Gstack 的本质：**
- 一个 **AI 工程工厂**，不是通用工具
- 13 个专家角色，都是斜杠命令
- 一个人 × gstack = 20 人团队的产出
- MIT 开源，永久免费，无付费层级

**适合场景：**
- 创始人需要快速验证想法
- 技术 CEO 想保持编码感觉
- 团队需要严格的 QA/评审/发布自动化
- 想要学习 AI-assisted 开发的人

**关键优势：**
- 结构化角色 vs 通用代理混乱
- 完整的 SDLC（规划→设计→实现→评审→QA→发布）
- 真实浏览器、真实 QA
- 100% 测试覆盖率目标
- 文档自动化
